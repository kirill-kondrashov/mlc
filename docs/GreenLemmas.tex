\section{Green's Function Lemmas}

\subsection*{Def escape\_bound}
\textbf{Description:} A bound used to ensure the orbit is large enough for the log approximation.

\begin{lstlisting}[language=Lean]
def escape_bound (c : ℂ) : ℝ := max (R c) (Real.sqrt (2 * ‖c‖ + 1))
\end{lstlisting}

\subsection*{Def potential\_seq}
\textbf{Description:} The n-th approximation of the Green's function: \texttt{1/2\^{}n * log (max 1 ‖f\_c\^{}n(z)‖)}.

\begin{lstlisting}[language=Lean]
def potential_seq (c z : ℂ) (n : ℕ) : ℝ :=
\end{lstlisting}

\subsection*{Def green\_function}
\textbf{Description:} The Green's function \texttt{G\_c(z)}. Defined as the limit of \texttt{potential\_seq}.

\begin{lstlisting}[language=Lean]
def green_function (c z : ℂ) : ℝ :=
\end{lstlisting}

\subsection*{Lemma potential\_seq\_converges\_of\_mem\_K}
\textbf{Description:} Convergence of the potential sequence to 0 for \texttt{z $\in$ K(c)}.

\begin{lstlisting}[language=Lean]
lemma potential_seq_converges_of_mem_K (h : z ∈ K c) :
    Tendsto (potential_seq c z) atTop (𝓝 0) := by
\end{lstlisting}

\subsection*{Lemma potential\_seq\_converges\_of\_escapes}
\textbf{Description:} Convergence of the potential sequence for \texttt{z ∉ K(c)}.

\begin{lstlisting}[language=Lean]
lemma potential_seq_converges_of_escapes (h : z ∉ K c) :
    ∃ L, Tendsto (potential_seq c z) atTop (𝓝 L) := by
\end{lstlisting}

\subsection*{Lemma potential\_seq\_converges}
\textbf{Description:} Convergence of the potential sequence for all \texttt{z}.

\begin{lstlisting}[language=Lean]
lemma potential_seq_converges (c z : ℂ) :
    ∃ L, Tendsto (potential_seq c z) atTop (𝓝 L) := by
\end{lstlisting}

\subsection*{Lemma green\_function\_eq\_lim}
\textbf{Description:} \texttt{G\_c(z)} equals the limit of the potential sequence.

\begin{lstlisting}[language=Lean]
lemma green_function_eq_lim (c z : ℂ) :
    Tendsto (potential_seq c z) atTop (𝓝 (green_function c z)) := by
\end{lstlisting}

