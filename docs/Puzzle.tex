\section{Puzzle Definitions}

\subsection*{Def DynamicalPuzzlePiece}
\textbf{Description:} The dynamical puzzle piece of depth n containing z.
    Definition: The connected component of \texttt{\{z | G(z) < 1/2\^{}n\}} containing \texttt{z}.
    These pieces form the basis of the Yoccoz puzzle construction.

\begin{lstlisting}[language=Lean]
def DynamicalPuzzlePiece (c : ℂ) (n : ℕ) (z : ℂ) : Set ℂ :=
\end{lstlisting}

\subsection*{Def PuzzleAnnulus}
\textbf{Description:} The annulus between two nested puzzle pieces around the critical point.
    Definition: The annulus between two nested puzzle pieces \texttt{P\_n \textbackslash\{\} P\_\{n+1\}}.
    The sum of their moduli determines renormalizability.

\begin{lstlisting}[language=Lean]
def PuzzleAnnulus (c : ℂ) (n : ℕ) : Set ℂ :=
\end{lstlisting}

\subsection*{Def ParaPuzzlePiece}
\textbf{Description:} A para-puzzle piece in the parameter plane.
    Definition: The set of parameters \texttt{c} for which the critical point 0 lies in the
    dynamical puzzle piece of depth \texttt{n}.

\begin{lstlisting}[language=Lean]
def ParaPuzzlePiece (n : ℕ) : Set ℂ := {c | c ∈ DynamicalPuzzlePiece c n 0}
\end{lstlisting}

