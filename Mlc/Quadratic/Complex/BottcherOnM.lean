import Mlc.Quadratic.Complex.BottcherMotion

namespace MLC

open Quadratic Complex Topology Set Filter

/-!
Sketch: existence of Böttcher coordinates on the Mandelbrot set.

This is a placeholder for the analytic/dynamical proof that constructs the
parameter disk and associated Böttcher data uniformly over `M`.
-/

def bottcher_onM_hyp : MLC.Quadratic.BottcherOnMHyp := by
  -- Build the `BottcherOnMHyp` data from a full Böttcher construction on `M`.
  -- The expected proof uses the global holomorphic motion / stability theory
  -- from `BottcherMotion` and the Lambda Lemma.
  sorry

end MLC
