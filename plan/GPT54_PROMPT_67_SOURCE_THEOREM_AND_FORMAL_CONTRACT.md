Begin the source-first moving-parapuzzle sequence with:

`plan/GPT54_TASK_67_SOURCE_THEOREM_AND_FORMAL_CONTRACT.md`

The direct frozen proof has terminated at a missing boundary-crossing theorem.
Do not continue that route. The new objective is to remove the frozen axiom by
formalizing genuine moving parapuzzle geometry.

Select one precise classical finite-level quadratic parapuzzle theorem with a
phase–parameter correspondence. Use a public, identifiable source and record:

- exact theorem statement and hypotheses;
- whether it concerns finitely renormalizable parameters, arbitrary finite
  levels, or only a restricted class;
- the definitions of the moving parameter windows;
- the theorem supplying openness, nesting/basis, and connected Mandelbrot
  slices;
- the exact phase–parameter transport statement.

Then design a Lean contract that is **not** just:

```lean
ConnectednessWindowParameterPieceData c W K
```

under a new name. The contract must expose substantive finite geometry, such as
finite boundary arcs/graphs, admissibility/combinatorics, a parameter-component
definition, and a transport map/homeomorphism.

Audit the repository for reusable prerequisites, but make no speculative source
edits. If the selected classical theorem cannot be stated non-opaquely with
current foundations, report the smallest foundational module required.

Do not use the frozen straddling axiom, `ParaPuzzlePieceAt`, `parameterSet` alone,
or placeholder `True` hypotheses. Do not commit.

Write:

`plan/GPT54_RESULT_67_SOURCE_THEOREM_AND_FORMAL_CONTRACT.md`
