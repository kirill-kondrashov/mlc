# Retired -- mathematically invalid target

An exterior domain such as `{z : ℂ | 1 < ‖z‖}` is not simply connected in
ordinary plane topology. Therefore the proposed implication to
`IsSimplyConnected Kᶜ` was false as stated.

The corrected route begins with:

`plan/GPT54_PROMPT_88_CORRECT_EXTERIOR_UNIFORMIZATION_TARGET.md`
