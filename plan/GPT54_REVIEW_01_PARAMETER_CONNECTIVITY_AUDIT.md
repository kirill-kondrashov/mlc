# Supervisor Review 01: Parameter-connectivity audit

**Verdict:** approved with one qualification.

The worker correctly substantiated the open-mapping obstruction to
`ParaPieceCarvedByMotion`. Slice holomorphy holds on an open superset, and slice
injectivity rules out local constancy on small balls in the open source. The
relative-clopen argument correctly proves that a connected source meeting both a
closed set and its complement cannot have an ambient-open intersection with it.

Qualification: replacing the source by an arbitrary non-open connected set only
avoids this contradiction. It does not make `ParaPieceIsMotionImage` a useful
reduction, because its exact-image existential can still package the target.

The task contract passed. No Lean source or existing plan was edited by the
worker.
