Complete the active-frontier task in
`plan/GPT54_TASK_54_PIN_GENUINE_FINITE_MOVING_PARAMETER_WINDOW.md`.

Result 53 corrected the consumer API: an open ambient parameter window `W n`
must be separated from the moving-family connectedness locus `K n`. The next
global gate is to identify one genuine finite-level moving parameter window that
can eventually instantiate that API.

Perform a feasibility audit against:

- the repository’s parameter-plane declarations;
- the existing `BMolParameterFamily` / analytic quadratic-like family core;
- the local source material in `refs/`.

Determine whether a concrete finite moving window can now be defined honestly
from existing checked objects. The preferred candidates are:

1. a connected component of the complement of a finite parameter
   ray/equipotential graph; or
2. an explicitly sourced proper unfolded quadratic-like-family parameter domain.

If a candidate is available, implement only the smallest concrete definition
and elementary API:

- the window as an actual `Set ℂ`;
- its basepoint membership;
- ambient openness if directly provable;
- the relation to a supplied moving family or graph.

Do not claim relative Mandelbrot connectedness, nesting, shrinkage, or
phase–parameter correspondence unless checked theorems provide them.

If no candidate is implementable because parameter rays/equipotentials or a
proper unfolded family are absent, do not create another abstract shell.
Report the exact first missing declaration/theorem and the smallest next
foundation task. Keep the corrected window/locus interface from Result 53
unchanged.

Write the worker report to:

`plan/GPT54_RESULT_54_PIN_GENUINE_FINITE_MOVING_PARAMETER_WINDOW.md`

Do not resume Böttcher mesh/monodromy work. Do not add `sorry`, `admit`, or new
axioms, do not edit unrelated files, and do not commit.
