# Gated -- run only after Result 98

`plan/GPT54_TASK_99_DISCHARGE_STRADDLING_FROM_MOVING_PROVIDER.md`

Use the genuine parameter arcs, moving-window topology, and phase--parameter
transport from Result 98 to instantiate the existing moving-parapuzzle
adapters. Prove:

```lean
green_sublevel_translate_inter_mandelbrot_connected_straddling
```

without its axiom, `external_ray_map_exists`, `mandelbrot_set_connected`, the
frozen direct-proof axiom, a new axiom, `sorry`, `admit`, or opaque provider
contracts. Inspect the target theorem's axiom dependencies before declaring
the replacement complete. Do not commit.

Write:

`plan/GPT54_RESULT_99_DISCHARGE_STRADDLING_FROM_MOVING_PROVIDER.md`
