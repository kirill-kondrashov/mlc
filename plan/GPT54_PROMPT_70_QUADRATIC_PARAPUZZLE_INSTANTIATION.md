Continue with:

`plan/GPT54_TASK_70_QUADRATIC_PARAPUZZLE_INSTANTIATION.md`

Instantiate the abstract finite parapuzzle topology and phase–parameter
connectedness transfer for the actual quadratic family.

Use genuine finite external-ray/equipotential/graph data or an equivalent
classical construction. Prove, for finitely renormalizable
`c ∈ MandelbrotSet`, a depth-indexed family of moving windows with:

- openness;
- `c` membership;
- basis/shrinkage;
- connectedness of each `W n ∩ MandelbrotSet`;
- a non-opaque phase–parameter correspondence.

Then add the smallest adapter to
`ConnectednessWindowParameterPieceData` and test construction of
`FiniteMovingWindowProviderData`.

If the quadratic ray/landing/graph prerequisites are absent, stop immediately
with the exact missing theorem and do not fabricate them. Do not use the frozen
straddling axiom or `ParaPuzzlePieceAt`. Do not commit.

Write:

`plan/GPT54_RESULT_70_QUADRATIC_PARAPUZZLE_INSTANTIATION.md`
