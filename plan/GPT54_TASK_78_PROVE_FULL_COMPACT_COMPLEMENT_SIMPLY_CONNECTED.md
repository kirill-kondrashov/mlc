# TASK 78 — Prove the full-compact complement bridge

## Objective

Establish the generic plane-topology implication from compact fullness to simple
connectedness of the complement, using the exact definitions selected in
Result 77.

## Requirements

The result must be a genuine theorem about subsets of `ℂ`, not a structure with
an opaque `isSimplyConnected` field. Reuse existing Mathlib notions and prove
all hypotheses explicitly.

If a classical planar theorem is needed but absent from Mathlib, report:

- exact statement;
- why current APIs do not prove it;
- whether it is independent of the Mandelbrot-specific problem.

## Constraints

- no external coordinate;
- no Riemann-map assumption;
- no frontier axiom;
- no new axiom, `sorry`, or `admit`;
- do not commit.

## Result

Write:

`plan/GPT54_RESULT_78_PROVE_FULL_COMPACT_COMPLEMENT_SIMPLY_CONNECTED.md`
